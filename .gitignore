node_modules/
dist/
build/
coverage/

# Secrets. Only .env.example is committed, and it holds placeholders.
.env
.env.*
!.env.example

# Prisma generates the client into the source tree; it is a build artefact,
# recreated by `npm run db:generate`.
/src/generated/

*.log
.DS_Store
